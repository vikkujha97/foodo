<?php
?> 
/*session_start();
include"header.php";
include "button.php";
include "database";
if(isset($_POST['save']))
{
  /*  if( $_SESSION['logged']==true)
    {
      $email=$_SESSION['user'];
      $firstn=$_POST['firstn'];
       $lastn=$_POST['lastn'];
       $usern=$_POST['usern'];
        $add1=$_POST['add1'];
          $add2=$_POST['add2']
         // $state=$_POST['state'];
          //$zip=$_POST['zip'];

       $q=mysql_query("select email from bio where email='$email'");
        $querry=mysql_query("UPDATE INTO  bio(firstn,lastn,usern,add1,add2,state,zip) values('$firstn','$lastn','$usern','$add1','$add2','$state','$zip')")or die(mysql_error());
        if($querry)
        {
          echo "Details saved successfully";
          header("refresh:02, location:profile.php");
        }
        else
          echo "failure";


    }
    
    }
    else
       echo "you havent logged";
     header("url=log.php");

?>
*/